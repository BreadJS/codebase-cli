package config

import (
	"github.com/codebase-foundation/cli/src/codebase/types"
)

// Config is an alias for types.Config for backward compatibility.
// New code should use types.Config directly.
type Config = types.Config
